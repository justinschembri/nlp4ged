# Configuration settings for nlp4ged.
from pathlib import Path
## DATAPATHS.

ROOT_PATH = Path(__file__).parent

DATA_PATH = ROOT_PATH / 'data'

REGEX_FILE_PATH = ROOT_PATH / 'nlp4ged' / 'regex' / 'regex_list.csv'